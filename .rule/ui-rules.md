# UI Rules

- NativeWind is the primary styling layer.
- primitives live under `components/ui/`.
- loading, empty, error, and offline-ish states must be considered.
- Hebrew RTL must not be a post-MVP retrofit.
