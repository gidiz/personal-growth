# Versioning Rules

- keep commits ticket-scoped.
- use PRs to protected branches.
- database migrations are immutable after release; fix forward with a new migration.
- release/tagging strategy may evolve later and should be recorded in an ADR when adopted.
